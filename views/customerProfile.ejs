<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Labour Link - Customer Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #3498db;
            --secondary: #2ecc71;
            --dark: #2c3e50;
            --light: #ecf0f1;
            --danger: #e74c3c;
            --warning: #f39c12;
            --gray: #95a5a6;
        }
        
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }
        
        .profile-header {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 30px 0;
            margin-bottom: 30px;
        }
        
        .profile-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            transition: transform 0.3s ease;
        }
        
        .profile-card:hover {
            transform: translateY(-5px);
        }
        
        .profile-avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background-color: var(--light);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
            color: var(--gray);
            margin-right: 30px;
            overflow: hidden;
            border: 5px solid white;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        
        .profile-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .info-group {
            margin-bottom: 15px;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        
        .info-group:last-child {
            border-bottom: none;
        }
        
        .info-group label {
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 5px;
            font-size: 0.9rem;
        }
        
        .order-item {
            border-left: 4px solid var(--primary);
            padding-left: 15px;
            margin-bottom: 20px;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 8px;
            transition: all 0.3s ease;
        }
        
        .order-item:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }
        
        .order-status {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            text-transform: capitalize;
        }
        
        .status-completed {
            background-color: var(--secondary);
            color: white;
        }
        
        .status-pending {
            background-color: var(--warning);
            color: white;
        }
        
        .status-cancelled {
            background-color: var(--danger);
            color: white;
        }
        
        .status-in-progress {
            background-color: var(--primary);
            color: white;
        }
        
        .section-title {
            color: var(--dark);
            border-bottom: 2px solid var(--light);
            padding-bottom: 10px;
            margin-bottom: 20px;
            font-weight: 600;
        }
        
        .btn-become-pro {
            background: linear-gradient(135deg, #f39c12, #e67e22);
            border: none;
            color: white;
            transition: all 0.3s ease;
        }
        
        .btn-become-pro:hover {
            background: linear-gradient(135deg, #e67e22, #d35400);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        }
        
        .stats-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 20px;
            text-align: center;
        }
        
        .stats-number {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 0;
        }
        
        .stats-label {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .quick-action-btn {
            height: 80px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            transition: all 0.3s ease;
        }
        
        .quick-action-btn:hover {
            transform: translateY(-3px);
        }
        
        .empty-state {
            padding: 40px 20px;
            text-align: center;
            color: #6c757d;
        }
        
        .empty-state i {
            font-size: 4rem;
            margin-bottom: 20px;
            opacity: 0.5;
        }
        
        @media (max-width: 768px) {
            .profile-avatar {
                width: 80px;
                height: 80px;
                font-size: 24px;
                margin-right: 15px;
                margin-bottom: 15px;
            }
            
            .profile-header .d-flex {
                flex-direction: column;
                text-align: center;
            }
            
            .btn-group-mobile {
                display: flex;
                flex-direction: column;
                gap: 10px;
            }
            
            .btn-group-mobile .btn {
                margin: 2px 0;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="profile-header">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    <div class="profile-avatar">
                        <% if (customer && customer.profileImage) { %>
                            <img src="<%= customer.profileImage %>" alt="<%= customer.name %>" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                            <i class="fas fa-user" style="display: none;"></i>
                        <% } else { %>
                            <i class="fas fa-user"></i>
                        <% } %>
                    </div>
                    <div>
                        <% if (customer) { %>
                            <h1 class="h3 mb-1"><%= customer.name %></h1>
                            <p class="mb-0">
                                <i class="fas fa-map-marker-alt me-1"></i>
                                <% if (customer.address && customer.address.city) { %>
                                    <%= customer.address.city %>, <%= customer.address.state || 'USA' %>
                                <% } else { %>
                                    Location not set
                                <% } %>
                            </p>
                            <p class="mb-0">
                                <i class="fas fa-user-tie me-1"></i>
                                Customer Account
                                <span class="badge bg-light text-dark ms-2">
                                    <i class="fas fa-star text-warning me-1"></i>
                                    Member since <%= customer.createdAt ? new Date(customer.createdAt).getFullYear() : '2024' %>
                                </span>
                            </p>
                        <% } else { %>
                            <h1 class="h3 mb-1">Customer Profile</h1>
                            <p class="mb-0">Please log in to view your profile</p>
                        <% } %>
                    </div>
                </div>
                <div class="btn-group-mobile">
                    <a href="/" class="btn btn-light me-2 mb-2">
                        <i class="fas fa-home me-1"></i> Home
                    </a>
                    <a href="/find-labour" class="btn btn-outline-light me-2 mb-2">
                        <i class="fas fa-search me-1"></i> Find Professionals
                    </a>
                    <a href="/logout" class="btn btn-outline-light mb-2">
                        <i class="fas fa-sign-out-alt me-1"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        <% if (customer) { %>
            <div class="row">
                <!-- Personal Information -->
                <div class="col-lg-6">
                    <div class="card profile-card">
                        <div class="card-body">
                            <h3 class="section-title">
                                <i class="fas fa-user-circle me-2"></i>Personal Information
                            </h3>
                            
                            <div class="info-group">
                                <label>Full Name</label>
                                <p class="mb-0 fs-5"><%= customer.name %></p>
                            </div>
                            
                            <div class="info-group">
                                <label>Age</label>
                                <p class="mb-0"><%= customer.age %> years</p>
                            </div>
                            
                            <div class="info-group">
                                <label>Email Address</label>
                                <p class="mb-0">
                                    <i class="fas fa-envelope me-2 text-primary"></i>
                                    <%= customer.email %>
                                </p>
                            </div>
                            
                            <div class="info-group">
                                <label>Phone Number</label>
                                <p class="mb-0">
                                    <i class="fas fa-phone me-2 text-primary"></i>
                                    <%= customer.phone %>
                                </p>
                            </div>
                            
                            <div class="info-group">
                                <label>Address</label>
                                <p class="mb-0">
                                    <i class="fas fa-map-marker-alt me-2 text-primary"></i>
                                    <% if (customer.address && customer.address.street) { %>
                                        <%= customer.address.street %>, 
                                        <%= customer.address.city %>, 
                                        <%= customer.address.state %> 
                                        <%= customer.address.zipCode %>
                                    <% } else { %>
                                        <span class="text-muted">Address not provided</span>
                                    <% } %>
                                </p>
                            </div>
                            
                            <div class="info-group">
                                <label>Member Since</label>
                                <p class="mb-0">
                                    <i class="fas fa-calendar me-2 text-primary"></i>
                                    <%= customer.createdAt ? new Date(customer.createdAt).toLocaleDateString() : 'Recently' %>
                                </p>
                            </div>
                            
                            <div class="mt-4">
                                <a href="/edit-profile" class="btn btn-primary">
                                    <i class="fas fa-edit me-2"></i>Edit Profile
                                </a>
                                <a href="/change-password" class="btn btn-outline-secondary ms-2">
                                    <i class="fas fa-lock me-2"></i>Change Password
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Statistics Card -->
                    <div class="card profile-card">
                        <div class="card-body">
                            <h3 class="section-title">
                                <i class="fas fa-chart-bar me-2"></i>Your Activity
                            </h3>
                            <div class="row text-center">
                                <div class="col-4">
                                    <div class="stats-card">
                                        <h3 class="stats-number">
                                            <%= orders ? orders.length : 0 %>
                                        </h3>
                                        <div class="stats-label">Total Orders</div>
                                    </div>
                                </div>
                                <div class="col-4">
                                    <div class="stats-card" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
                                        <h3 class="stats-number">
                                            <%= orders ? orders.filter(order => order.status === 'completed').length : 0 %>
                                        </h3>
                                        <div class="stats-label">Completed</div>
                                    </div>
                                </div>
                                <div class="col-4">
                                    <div class="stats-card" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);">
                                        <h3 class="stats-number">
                                            <%= orders ? orders.filter(order => order.status === 'pending').length : 0 %>
                                        </h3>
                                        <div class="stats-label">Pending</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Previous Orders -->
                <div class="col-lg-6">
                    <div class="card profile-card">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h3 class="section-title mb-0">
                                    <i class="fas fa-history me-2"></i>Order History
                                </h3>
                                <span class="badge bg-primary">
                                    <%= orders ? orders.length : 0 %> orders
                                </span>
                            </div>
                            
                            <% if (orders && orders.length > 0) { %>
                                <div class="orders-list">
                                    <% orders.forEach(function(order) { %>
                                        <div class="order-item">
                                            <div class="d-flex justify-content-between align-items-start mb-2">
                                                <div class="flex-grow-1">
                                                    <h5 class="mb-1 text-dark"><%= order.serviceType || order.service || 'Service' %></h5>
                                                    <p class="text-muted mb-1">
                                                        <i class="fas fa-user me-1"></i>
                                                        <% if (order.labourId && order.labourId.name) { %>
                                                            <%= order.labourId.name %>
                                                            <% if (order.labourId.profession) { %>
                                                                - <%= order.labourId.profession %>
                                                            <% } %>
                                                        <% } else { %>
                                                            Professional not assigned
                                                        <% } %>
                                                    </p>
                                                    <p class="text-muted mb-1">
                                                        <i class="fas fa-calendar me-1"></i>
                                                        <%= new Date(order.createdAt).toLocaleDateString() %>
                                                    </p>
                                                    <% if (order.amount) { %>
                                                        <p class="text-success mb-0 fw-bold">
                                                            <i class="fas fa-rupee-sign me-1"></i>
                                                            ₹<%= order.amount %>
                                                        </p>
                                                    <% } %>
                                                </div>
                                                <div class="order-status status-<%= order.status %> ms-3">
                                                    <%= order.status %>
                                                </div>
                                            </div>
                                            <% if (order.description) { %>
                                                <p class="mt-2 mb-0 text-muted"><%= order.description %></p>
                                            <% } %>
                                            <% if (order.status === 'completed' && order.customerRating) { %>
                                                <div class="mt-2">
                                                    <small class="text-warning">
                                                        <% for (let i = 0; i < 5; i++) { %>
                                                            <i class="fas fa-star<%= i < order.customerRating ? '' : '-half-alt' %>"></i>
                                                        <% } %>
                                                        Your rating: <%= order.customerRating %>/5
                                                    </small>
                                                </div>
                                            <% } %>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <div class="empty-state">
                                    <i class="fas fa-shopping-cart"></i>
                                    <h4>No Orders Yet</h4>
                                    <p class="text-muted">You haven't placed any orders yet. Start by finding a professional for your needs.</p>
                                    <a href="/find-labour" class="btn btn-primary">
                                        <i class="fas fa-search me-2"></i>Find Professionals
                                    </a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                    
                    <!-- Quick Actions -->
                    <div class="card profile-card">
                        <div class="card-body">
                            <h3 class="section-title">
                                <i class="fas fa-bolt me-2"></i>Quick Actions
                            </h3>
                            <div class="row g-3">
                                <div class="col-6">
                                    <a href="/find-labour" class="btn btn-outline-primary w-100 quick-action-btn">
                                        <i class="fas fa-search fa-2x mb-2"></i>
                                        <span>Find Professionals</span>
                                    </a>
                                </div>
                                <div class="col-6">
                                    <a href="#" 
                                       class="btn btn-become-pro w-100 quick-action-btn" 
                                       onclick="confirmLogoutAndRedirect(event)">
                                        <i class="fas fa-tools fa-2x mb-2"></i>
                                        <span>Become Professional</span>
                                    </a>
                                </div>
                                <div class="col-6">
                                    <a href="/edit-profile" class="btn btn-outline-secondary w-100 quick-action-btn">
                                        <i class="fas fa-edit fa-2x mb-2"></i>
                                        <span>Edit Profile</span>
                                    </a>
                                </div>
                                <div class="col-6">
                                    <a href="/help" class="btn btn-outline-info w-100 quick-action-btn">
                                        <i class="fas fa-question-circle fa-2x mb-2"></i>
                                        <span>Help & Support</span>
                                    </a>
                                </div>
                            </div>
                            
                            <!-- Additional Info Text -->
                            <div class="mt-3 text-center">
                                <small class="text-muted">
                                    <i class="fas fa-info-circle me-1"></i>
                                    Want to offer your services? Click "Become Professional" to register as a skilled worker.
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="card profile-card">
                <div class="card-body text-center py-5">
                    <i class="fas fa-exclamation-triangle fa-3x text-warning mb-3"></i>
                    <h3>Profile Not Found</h3>
                    <p class="text-muted">Unable to load customer profile. Please try logging in again.</p>
                    <div class="mt-3">
                        <a href="/login" class="btn btn-primary me-2">Go to Login</a>
                        <a href="/" class="btn btn-outline-secondary">Return Home</a>
                    </div>
                </div>
            </div>
        <% } %>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Labour Link</h5>
                    <p class="mb-0">Connecting customers with skilled labour in their vicinity</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0">&copy; 2025 Labour Link. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function confirmLogoutAndRedirect(event) {
            event.preventDefault();
            if (confirm("You will be logged out and redirected to register as a Professional. Continue?")) {
                window.location.href = "/logout?redirect=labour";
            }
        }

        // Image error handling
        document.addEventListener('DOMContentLoaded', function() {
            const images = document.querySelectorAll('img');
            images.forEach(img => {
                img.addEventListener('error', function() {
                    this.style.display = 'none';
                    const fallback = this.nextElementSibling;
                    if (fallback && fallback.classList.contains('fas')) {
                        fallback.style.display = 'flex';
                    }
                });
            });
        });
    </script>
</body>
</html>